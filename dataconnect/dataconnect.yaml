specVersion: "v1"
serviceId: "react-tg-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "react-tg-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
